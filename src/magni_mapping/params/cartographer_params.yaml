cartographer_node:
  ros__parameters:
    use_sim_time: true
    configuration_directory: "$(find magni_mapping)/config"
    configuration_basename: "magni_2d.lua"
    publish_tracked_pose: true

occupancy_grid:
  ros__parameters:
    resolution: 0.05
    publish_period_sec: 1.0
